#include<stdio.h>
int main (){
	int n=0;
	int sum=0;
	double aver=0;
	scanf("%d",&n);
	int i=0,d=0;
	for(i=0;i<n;i++){
		scanf("%d",&d);
		sum+=d;
		
		
	}
	aver=1.0*sum/n;
	printf("%d ",sum);
	printf("%.5f",aver);
	return 0;
}
